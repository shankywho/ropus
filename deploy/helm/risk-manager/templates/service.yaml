apiVersion: v1
kind: Service
metadata:
  name: {{ include "risk-manager.fullname" . }}-svc
  labels:
    {{- include "risk-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "risk-manager.selectorLabels" . | nindent 4 }}
