apiVersion: v1
kind: Service
metadata:
  name: risk-backend-svc
  namespace: risk-engine
  labels:
    app.kubernetes.io/name: risk-backend
    app.kubernetes.io/part-of: ai-risk-manager
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: risk-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
